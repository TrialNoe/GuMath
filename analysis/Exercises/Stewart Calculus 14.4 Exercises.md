## 1 证明可微性蕴含连续性

> [!question]
> Prove that if $f$ is a function of two variables that is differentiable at $(a, b)$, then $f$ is continuous at $(a, b)$.
> Hint: Show that
> $$
> \lim_{(\Delta x, \Delta y) \to (0, 0)} f(a + \Delta x, b + \Delta y) = f(a, b)
> $$
> 



## 2 函数在原点的偏导数存在但不可微

> [!question]
> (a) The function
> $$
> f(x, y) = 
> \begin{cases}
> \dfrac{xy}{x^2 + y^2} & \text{if } (x, y) \neq (0, 0) \\
> 0 & \text{if } (x, y) = (0, 0)
> \end{cases}
> $$
> was graphed in Figure 4. Show that $f_x(0, 0)$ and $f_y(0, 0)$ both exist but $f$ is not differentiable at $(0, 0)$. [Hint: Use the result of Exercise 53.]
> 
> (b) Explain why $f_x$ and $f_y$ are not continuous at $(0, 0)$.
> 

